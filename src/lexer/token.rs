// TODO custom attribute macro that recognizes each symbol from a string
#[derive(Debug, PartialEq)]
pub enum Token {
    Illegal(String),
    EOF,
    Identifier(String),
    Int(i64),
    Assign,
    Plus,
    Minus,
    Bang,
    Asterisk,
    Slash,
    Lt,
    Gt,
    Comma,
    Semicolon,
    Lparen,
    Rparen,
    Lbrace,
    Rbrace,
    Fn,
    Let,
    Eq,
    Neq,
    If,
    Else,
    Return,
    True,
    False,
}
